# `base` Control Pack

Engineering completeness controls. Not a Singapore legal conclusion and not a certification.

Preview rules are declarative JSON under `rules/`. The Core evaluator interprets them; this pack does not ship executable rule code.

Locked in generated projects as `id: base`, `version: 0.0.0`, `digest: sha256:preview-unsigned`.
