# Claude Code adapter

Follow `AGENTS.md`. It is the source of truth for this repository.

Do not create a parallel instruction set here. If you need a project-specific note, add a dated file under `docs/agents/` and link it from `AGENTS.md`.
