# Plan — repair generated npm installation

The human requested dependency repair and publishing according to repository rules, then confirmed continuation in response to the generator takeover question. The generator claim is transferred to this task; `feat/generator-add-tool-docs` and its clean worktree remain preserved. Core/contracts claims are unchanged.

1. In `packages/project-generator/src/dependencies.ts` and `generate.ts`, make published, version-pinned dependencies the default. Require contracts/runtime directly and the project CLI as a dev dependency; omit checkout paths and local overrides. Keep an explicit library-only `dependencyMode: "local-file"` for repository fixtures, with no additional create CLI flag or user wizard.
2. Fix the generated npm script to invoke the installed CLI binary. Include pinned TypeScript/Node types and working build/typecheck scripts. Emit a real default gateway eval using the existing CLI ResultState convention. Update stale README/workflow comments while retaining manual-only workflow activation and all existing control checks. Deliberately review snapshot changes; preserve paths and ownership classes.
3. Prepare generator `0.1.1`, set CLI/bootstrap dependency minimum to that fixed version, and synchronize workspace lock entries. Keep CLI candidate `0.1.1`; none of these candidates have been published. Do not copy unrelated old generator add-tool changes into this dependency repair.
4. Update generator tests and CLI fixtures: local linking remains explicitly tested, published output must contain no file paths/overrides, install/build/typecheck/dev/default eval must succeed. Static added evals stay UNKNOWN until implemented; do not suppress Core findings for user-owned edits.
5. Add `packages/cli/verification/distribution.mjs`: pack built workspaces, serve only candidate npm metadata/tarballs via a temporary loopback registry with public npm fallback, install the packed bootstrap in isolation, create the default project, install its unmodified dependencies, and run the full build/test/CLI flow. Remove the bootstrap installation before project execution to prove independence. Replace the repository generated-project CI shell fixture with this distribution proof; keep main-only publish gating. The test registry is temporary test infrastructure, not a product service.
6. Verify root build/test/typecheck/audit, Node 22, workflow syntax, and distribution proof. Commit/push evidence and keep human review/merge outstanding. The current GitHub connector cannot create a PR (403); do not bypass permissions or self-approve a merge.

Scope excludes Core ownership semantics, full upgrade coverage, runtime schema resolution, automatic generated workflow triggers, adoption and full upgrade apply. Existing acceptance findings in those areas remain open. This closes dependency/build/default-eval failures, not full product conformance.
