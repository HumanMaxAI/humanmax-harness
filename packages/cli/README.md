# `@humanmax/cli`

Project-pinned `humanmax` executable. Preview commands: `dev`, `add`, `generate`, `upgrade`, `test`, `doctor`, `check`.

The CLI owns filesystem discovery and writes. It does not reimplement Core rules.
