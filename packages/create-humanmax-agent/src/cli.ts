const usage = `create-humanmax-agent <project>

Scaffold stub. Use --defaults when the generator is implemented.

This binary does not write a project yet.
`;

process.stderr.write(usage);
process.exitCode = 2;
