# `base` profile

Provider-neutral safe defaults. Always applied. Does not grant production authority.

The Preview generator pins this profile in `.humanmax/project.yaml` and `.humanmax/packs.lock`. It does not add `sg-core`.
